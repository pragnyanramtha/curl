# Rust curl parity roadmap

Status values:

- `verified`: implemented and covered by focused Rust tests plus mapped curl tests.
- `partial`: implemented for a limited subset, with known curl behavior missing.
- `missing`: not implemented in the Rust sidecar.
- `blocked`: blocked by architecture, dependencies, or test environment.

The Rust sidecar is incomplete until every required protocol, option, transfer
behavior, and test mapping is `verified` or explicitly accepted as out of scope.

## Source of truth

- CLI options: `src/tool_getparam.c`, `src/tool_parsecfg.c`,
  `docs/cmdline-opts/`
- Transfer behavior: `src/tool_operate.c`, `src/tool_cfgable.c`,
  `src/tool_cb_hdr.c`, `src/tool_cb_wrt.c`, `lib/`
- Protocol behavior: `lib/`, `docs/FEATURES.md`, `README.md`
- Tests: `tests/data/`, `tests/http/`, `tests/unit/`, `tests/libtest/`

## Protocol parity

| Protocol | Status | Rust entry point | Required behavior/tests | Notes |
| --- | --- | --- | --- | --- |
| DICT | partial | `transfer.rs::run_dict_transfer` | `dict_define_outputs_server_lines_and_sends_request`, `dict_aliases_preserve_case_and_ignore_extra_fields`, `dict_match_alias_sends_database_strategy_and_word`, `dict_empty_fields_use_curl_defaults`, `dict_generic_command_replaces_colons_with_spaces`, `dict_percent_decodes_and_escapes_words`, `dict_query_is_ignored_when_building_request`, `dict_head_sends_request_without_output_body`, `dict_dump_header_creates_empty_file`, `dict_dump_header_dash_writes_no_extra_output`, `dict_include_does_not_add_header_bytes`, `dict_writeout_reports_zero_http_code_and_download_size`, `dict_remote_name_writes_url_filename`, `dict_rejects_decoded_control_path`, `version_lists_dict_protocol` | Plain `dict://` TCP command send/read support only; DICT response lines are body data, `--dump-header` creates empty header output, `--include` does not add bytes, `%{header_json}` stays empty, and buffered `--max-filesize` limiting is shared with the plain TCP body path; auth, proxying, timeout/read cancellation parity, Rust-binary corpus mapping, and exact C `CLIENT libcurl` token parity remain incomplete |
| FILE | partial | `transfer.rs::run_file_transfer` | file URL, headers, range, output modes, `file_upload_writes_local_target`, `file_upload_missing_parent_returns_write_error`, curl corpus smoke tests 200, 201, 202, 203, 204, 205, 231, 1016, 1017, 1018, 1019, 1020, 1220, 2072 | GET/HEAD, simple ranges, resume to output files, local file uploads from `--upload-file`, query/fragment stripping, Unix path slash handling, case-insensitive `file` scheme dispatch, single-slash absolute `file:` URLs, missing source exit 37, write failure exit 23, and buffered `--max-filesize` body limiting only |
| HTTP | partial | `transfer.rs::run_http_transfer` | `downloads_http_and_renders_writeout`, `compressed_decodes_supported_response_bodies`, `compressed_custom_accept_encoding_still_decodes_response_body`, `compressed_include_preserves_encoded_response_headers`, `compressed_dump_header_preserves_encoded_response_headers`, `compressed_unknown_content_encoding_returns_bad_content_encoding`, `raw_http_compressed_request_target_decodes_body_and_preserves_headers`, `http_does_not_decode_content_encoding_without_compressed`, `default_http_get_decodes_chunked_response`, `custom_user_agent_and_accept_headers_suppress_defaults`, `empty_custom_headers_suppress_defaults_and_semicolon_sends_blank`, `header_file_uses_empty_and_semicolon_custom_header_semantics`, `generated_headers_respect_non_empty_custom_overrides`, `range_and_time_condition_headers_respect_custom_overrides`, `user_agent_option_sets_header_and_empty_value_suppresses_default`, `raw_proxy_uses_user_agent_option_and_empty_value_suppresses_default`, `raw_proxy_empty_custom_headers_suppress_defaults_and_semicolon_sends_blank`, `request_target_sets_direct_http_request_line`, `request_target_sets_http_proxy_request_line`, `http09_allows_headerless_response`, `http09_response_is_denied_by_default`, `no_http09_denies_headerless_response_after_enable`, `http09_denial_with_writeout_uses_unsupported_protocol_exit`, `raw_http09_response_requires_opt_in`, `raw_http09_allows_headerless_response`, `raw_http09_head_is_weird_reply`, `auto_referer_redirect_rewrites_post_to_get`, `post301_preserves_post_on_redirect`, `post302_preserves_post_on_redirect`, `post303_preserves_post_on_redirect`, `post301_follows_300_redirect_and_replays_post_body`, `custom_method_post_redirect_drops_body_when_post_flag_does_not_apply`, HTTP test corpus mapping | reqwest-backed subset; default `User-Agent: curl/<version>` and `Accept: */*` are covered along with `-A`/custom header suppression, empty `-A` omission, plain HTTP/raw proxy `-H Name:` suppression and `-H Name;` blank emission, generated-header overrides for Cookie, Authorization, If-None-Match, Range, If-Modified-Since, Content-Length, Accept-Encoding, raw proxy user-agent handling, plain HTTP `--request-target` request-line overrides, normal chunked HTTP/1.1 decoding on the reqwest path, `--compressed` gzip/deflate/brotli body decoding with encoded header preservation for `--include`/`--dump-header`, opt-in HTTP/0.9 headerless response handling for reqwest/raw paths with response code `000`, default/explicit-deny HTTP/0.9 failures including write-out exit reporting, JSON `Accept: application/json`, basic GET resume validation, default 301/302 POST-to-GET rewrite in the manual auto-referer redirect path, manual redirect following across 300-399 responses with `Location`, `--post301`/`--post302`/`--post303` POST preservation for followed redirects, custom-method POST redirect body dropping when a post-redir flag does not apply, and `--max-filesize` known `Content-Length` preflight plus buffered unknown-size body truncation. HTTPS and redirect cases for exact `-H Name:` omission, proxy-header parity, raw wire casing/order, overflow `Content-Length`, exact streaming, request-target outside the raw plain HTTP path, redirect auth/cookie scoping, retry/redirect resume edge cases, and broader decompression parity remain incomplete |
| HTTPS | partial | `transfer.rs::run_http_transfer` | TLS/proxy/cert tests | rustls-backed subset |
| FTP/FTPS | partial | `transfer.rs::run_ftp_transfer` | `ftp_retr_downloads_file_and_sends_default_sequence`, `ftp_directory_url_lists_with_type_a_and_list`, `ftp_list_only_uses_nlst_without_cwd_for_root`, `ftp_uses_url_userinfo_and_falls_back_to_pasv`, `ftp_230_greeting_skips_user_and_password`, `ftp_double_slash_cwds_to_root_before_file`, `ftp_head_outputs_synthetic_file_headers_without_data_connection`, `ftp_head_directory_only_logs_in_and_cwds`, `ftp_writeout_reports_transfer_code_and_download_size`, FTP upload tests, FTP resume tests, FTP create-dirs tests, FTP quote/prequote/postquote tests, `ftp_default_skips_pasv_ip`, `ftp_epsv_connect_failure_falls_back_to_pasv`, `ftp_disable_epsv_uses_pasv_directly`, `ftp_epsv_reenables_after_disable_epsv`, `ftp_ipv6_disable_epsv_still_uses_epsv`, `ftp_skip_pasv_ip_uses_control_host`, `ftp_max_filesize_rejects_after_size_before_retr`, `ftp_dump_header_writes_control_replies_and_include_adds_no_bytes`, `ftp_login_failure_returns_login_denied`, `ftp_cwd_failure_returns_remote_access_denied`, `ftp_pwd_failure_is_ignored_before_download`, `ftp_passive_failure_returns_13`, `ftp_malformed_pasv_reply_returns_14`, `ftp_type_failure_returns_17`, `ftp_retr_550_returns_remote_file_not_found`, `ftp_list_failure_returns_19`, `ftp_rejects_decoded_control_path`, `version_lists_ftp_protocol`, curl corpus 100, 102, 104, 107, 113, 114, 115, 117, 118, 120, 121, 124, 125, 126, 136, 137, 138, 147, 148, 225, 226, 227, 237, 238, 261, 290, 291, 295, 754, 1000, 1003, 1005, 1006, 1062, 1153, 1219, 1224, 1282, 2045 | Plain passive `ftp://` TCP support only; sends anonymous defaults or URL/`--user` credentials unless a `230` greeting reports an already authenticated control connection, sends `PWD` without treating a failed reply as fatal, sends `QUIT` before returning failed `CWD`, supports multi-CWD URL path traversal including `//` root CWD semantics, creates missing path components with `--ftp-create-dirs` using C curl's retry-mode `CWD`/`MKD`/`CWD` flow for downloads and uploads, EPSV with PASV fallback including fallback after EPSV data-connect failure, default PASV host-IP skipping, explicit `--disable-epsv`/`--epsv`, IPv6 EPSV forcing, `--ftp-skip-pasv-ip`, malformed `227` exit 14, binary `TYPE I`/`SIZE`/`RETR` file downloads, fixed and auto download resume via `SIZE`/`REST` with output-file appending, stdout resumes, unknown-size REST fallback, known-size already-complete/bad-resume handling, and REST failure mapping, passive binary uploads via `STOR`/`APPE` from files or stdin with directory URL filename append, fixed and auto upload resume via local byte skipping plus `APPE`/`SIZE`, ASCII `TYPE A`/`LIST` directory listings, `--list-only`/`NLST`, `-Q/--quote` normal, prequote, and postquote command phases with `*` ignored failures, prequote for `-I` file info requests, postquote only after successful transfer and local output/max-filesize handling, file `-I` fake headers via `MDTM`/`SIZE`/`REST 0`, directory `-I` without data transfer commands, FTP control replies for `--dump-header`, body-only `-i`, `%{response_code}` from FTP replies, `%{size_download}`, `--max-filesize` SIZE preflight plus buffered limiting, and exit codes 9/13/14/17/19/21/25/31/36/67/70/78 for covered failures. The mapped corpus also covers URL control-character rejection, no-password URL userinfo, USER/PASS login failures including unusual auth reply bytes, EPSV/PASV failure handling, TYPE/RETR failures, quoted path components, large/multiple control replies, FTP create-dirs retry behavior, quote/prequote/postquote protocol ordering for file downloads and listings, and passive upload smoke cases. `ftps://`, active mode, finite FTP byte ranges, ASCII/CRLF upload conversion, time-conditioned uploads, `--ftp-*` options beyond covered passive/create-dirs/login defaults, custom FTP requests beyond the mapped `-Q` command lists, wildcarding, proxying, connection reuse/stateful EPSV disabling, precise `-D -` interleaving, broader corpus fixture mapping, exact `--libcurl` source formatting for quote corpus 1405, and full command/error diagnostics remain incomplete |
| GOPHER/GOPHERS | partial | `transfer.rs::run_gopher_transfer` | `downloads_gopher_selector`, `gopher_decodes_selector_before_sending`, `gopher_degenerate_selector_sends_only_crlf`, `gopher_ipv6_literal_sends_selector`, `gopher_appends_and_decodes_query_component`, `gopher_writeout_reports_zero_http_code_and_download_size`, `gopher_remote_name_writes_url_filename`, `gopher_rejects_decoded_nul_selector`, `gopher_include_does_not_echo_selector_header_data`, `gopher_dump_header_writes_selector_as_header_data`, `gopher_dump_header_dash_writes_selector_before_body`, `gopher_head_sends_selector_without_output_body`, `gopher_head_dump_header_writes_selector_without_body`, `gopher_header_json_remains_empty`, curl corpus 1200, 1201, 1202, 1203 | Plain `gopher://` selector send/read support only; request selector is surfaced as raw header callback data for `--dump-header`, `--include` does not echo it, structured `%{header_json}` remains empty, IPv4 and bracketed IPv6 literal hosts are supported, and buffered `--max-filesize` limiting is shared with the plain TCP body path; `gophers://`, proxying, redirects, timeout parity, and full corpus mapping remain incomplete |
| IPFS/IPNS | partial | `ipfs.rs::maybe_rewrite_url` plus HTTP transfer path | `ipfs_gateway_rewrites_to_http_path`, `ipfs_path_and_query_with_gateway_path`, `ipns_path_and_query_with_gateway_path`, `ipfs_gateway_env_overrides_gateway_file`, `ipfs_gateway_file_discovery_uses_first_line`, `ipfs_path_env_discovery_accepts_no_trailing_slash`, `ipfs_gateway_query_is_malformed_exit_3`, `ipfs_auto_gateway_missing_exits_37`, `ipfs_auto_gateway_malformed_file_host_exits_3`, `ipfs_malformed_explicit_gateway_exits_43`, `ipfs_malformed_explicit_gateway_host_exits_43`, `libcurl_rewrites_ipfs_url_to_gateway_url`, `version_lists_ipfs_and_ipns_protocols`, curl corpus 723, 725, 726, 738, 739, 741 | Rewrites `ipfs://` and `ipns://` through explicit/env/file gateway discovery into the existing HTTP path, preserving path/query and gateway-path joins; gateway URL query rejection, generated libcurl URL rewriting, comma-host gateway rejection, and key exit codes are covered. Request-path IPFS corpus cases are not mapped yet because the reqwest-backed HTTP path emits lowercase header names/order unlike the C curl protocol expectations; full C URL encoding parity, trustless gateway concerns, full `IPFS_PATH` matrix, and exact C `curl -V` protocol formatting remain incomplete |
| IMAP/IMAPS | partial | `transfer.rs::run_imap_transfer` | `imap_fetch_mailindex_outputs_literal_and_quotes_login_atoms`, `imap_uses_url_userinfo_and_uid_section_fetch`, `imap_lists_mailbox_without_select`, `imap_skips_initial_banner_before_greeting`, `imap_search_uses_url_query_after_select`, `imap_custom_noop_without_mailbox_outputs_untagged_lines`, `imap_without_credentials_skips_login`, `imap_custom_fetch_outputs_envelope_and_literal`, `imap_custom_fetch_suppresses_literal_trailer_and_ignores_quoted_braces`, `imap_custom_command_failure_exits_quote_error`, `imap_uidvalidity_mismatch_exits_remote_file_not_found`, `imap_login_failure_returns_login_denied`, `imap_rejects_decoded_control_path`, `version_lists_imap_protocol`, curl corpus 800, 801, 802, 803, 806, 807, 808, 809, 810, 811, 812, 813, 814, 817, 818, 829, 841, 846, 847, 1847, 1848, 3206 | Plain `imap://` TCP support only; sends CAPABILITY, cleartext LOGIN with IMAP atom quoting, LIST, SELECT, FETCH/UID FETCH with `MAILINDEX`/`UID` and `SECTION`, SEARCH from URL query, simple custom `-X` commands with quote-error exit 21, custom FETCH envelope output, unauthenticated command flow when no credentials are supplied, skips initial corpus banner lines before `OK`/`PREAUTH` greetings, literal body extraction, URL userinfo, UIDVALIDITY mismatch exit 78, login failure exit 67, empty protocol headers, `%{http_code}` zero, `--max-filesize`, and decoded control-byte URL rejection. `imaps://`, STARTTLS, SASL/OAuth/`--login-options`, broader PREAUTH corpus coverage, APPEND/uploads/`--upload-flags`, custom FETCH listing details, mailbox reuse, proxying, protocol header dump parity, connection reuse, and full corpus mapping remain incomplete |
| LDAP/LDAPS | partial | `transfer.rs::run_ldap_transfer` | `ldap_anonymous_search_outputs_entry`, `ldap_url_parses_base_attrs_scope_and_filter`, `ldap_simple_bind_uses_user_option`, `ldap_search_failure_returns_39_and_writeout_code`, `ldap_bind_failure_returns_38`, `ldap_head_suppresses_body`, `ldap_dump_header_is_empty_and_include_adds_no_headers`, `version_lists_ldap_protocol` | Plain `ldap://` TCP support only; sends LDAPv3 simple bind, performs BER search requests with DN/attribute/scope/filter parsing for presence and equality filters, supports anonymous and `--user` simple credentials, outputs basic text attribute values as LDAP body data, keeps headers empty for `--dump-header`/`--include`, reports LDAP search result codes, supports `--max-filesize`, and maps bind/search failures to exit codes 38/39 for covered cases. `ldaps://`, STARTTLS, SASL/OAuth/login options, LDAPv2 fallback, referrals, controls/extensions, complex filters, binary/base64 LDIF formatting, paged searches, proxying, connection reuse, exact OpenLDAP error mapping, and full corpus mapping remain incomplete |
| MQTT/MQTTS | partial | `transfer.rs::run_mqtt_transfer` | `mqtt_subscribe_outputs_publish_packet_and_sends_subscribe`, `mqtt_publish_sends_data_payload_and_disconnects`, `mqtt_publish_empty_payload_to_space_topic`, `mqtt_connect_includes_user_and_password`, `mqtt_rejects_missing_topic_after_connack`, `mqtt_connack_error_returns_weird_server_reply`, `mqtt_max_filesize_rejects_large_publish_before_output`, `version_lists_mqtt_protocol`, curl corpus 1190, 1191, 1192, 1193, 1194, 1196, 1198, 1199, 2200, 2201, 2202, 2203, 2204, 2206, 2207, 3017, 3018 | Plain `mqtt://` TCP support only; sends MQTT 3.1.1 CONNECT with clean session and optional `--user`, supports QoS 0 SUBSCRIBE for downloads, QoS 0 PUBLISH via `--data`, percent-decoded URL topics, empty publish payloads when a topic exists, CONNACK/SUBACK validation, DISCONNECT handling, body output as MQTT PUBLISH remaining bytes, `--max-filesize` rejection against incoming PUBLISH remaining length, and mapped corpus coverage for long topics/payloads, auth failure paths, malformed PINGRESP/DISCONNECT packets, and oversized remaining lengths. `mqtts://`, random C client-id parity, remaining malformed packet cases including 1132/1195, ping/upkeep, proxying, streaming partial-file handling, and full corpus mapping remain incomplete |
| POP3/POP3S | partial | `transfer.rs::run_pop3_transfer` | `pop3_retr_downloads_message_and_unstuffs_dot_lines`, `pop3_skips_initial_banner_before_greeting`, `pop3_uses_url_userinfo_when_user_option_is_absent`, `pop3_empty_path_lists_messages`, `pop3_list_only_sends_list_for_message_id_without_body`, `pop3_command_error_returns_weird_server_reply`, `pop3_retr_command_error_sends_quit_and_returns_weird_server_reply`, `pop3_custom_top_outputs_multiline_body`, `pop3_head_custom_stat_suppresses_body_and_keeps_zero_http_code`, `pop3_rejects_decoded_control_path`, `pop3_login_failure_returns_login_denied`, `version_lists_pop3_protocol`, curl corpus 850, 851, 852, 853, 854, 855, 856, 857, 858, 859, 860, 861, 862, 863, 875, 894, 993, 997 | Plain `pop3://` TCP support only; sends CAPA, USER/PASS cleartext auth, LIST/RETR/default and custom `-X` commands, sends QUIT before returning command-level `-ERR` failures, skips initial corpus banner lines before `+OK`/`-ERR` greetings, handles multiline terminators and dot-stuffing, keeps POP3 protocol replies out of headers and `%{http_code}`, supports URL userinfo, `--list-only`, `--max-filesize`, and exit codes 8/67 for command/login failures. `pop3s://`, STLS, SASL/APOP/OAuth, URL `;AUTH=`, proxying, connection reuse, exact command-reply diagnostics, and full corpus mapping remain incomplete |
| RTSP | partial | `transfer.rs::run_rtsp_transfer` | `rtsp_default_options_sends_star_uri_and_cseq`, `rtsp_include_and_head_output_response_headers`, `rtsp_writeout_reports_response_code_and_zero_download_size`, `rtsp_custom_request_is_ignored_for_default_cli`, `rtsp_bad_status_line_exits_weird_server_reply`, `rtsp_missing_cseq_exits_85_after_parsing_response_code`, `rtsp_rejects_custom_cseq_header`, `version_lists_rtsp_protocol` | Plain `rtsp://` default CLI OPTIONS support only; sends `OPTIONS * RTSP/1.0` with `CSeq: 1`, default/custom user-agent and generic custom headers, validates response status and CSeq, reports RTSP response code, keeps OPTIONS body out of normal output and `size_download`, supports `-i`/`-I`/`--dump-header` header output, ignores `-X` like C CLI, rejects custom `CSeq:` with exit 85 and `Session:` with exit 43. RTSP stream URI setopt parity, method enum support (`DESCRIBE`, `ANNOUNCE`, `SETUP`, `PLAY`, `PAUSE`, `TEARDOWN`, `GET_PARAMETER`, `SET_PARAMETER`, `RECORD`, `RECEIVE`), request bodies, session capture/reuse/validation, `Transport:`, RTP interleaving, auth, proxying, connection reuse, range/time-condition handling, and full corpus mapping remain incomplete |
| SCP/SFTP | partial | `transfer.rs::run_scp_transfer`, `transfer.rs::run_sftp_transfer` | `sftp_downloads_file_with_known_hosts`, `scp_downloads_file_with_known_hosts`, `scp_quote_options_are_ignored`, `scp_unknown_quote_is_ignored`, SCP upload tests, SCP range/resume tests, `sftp_missing_file_returns_78`, `sftp_bad_key_returns_login_denied`, `sftp_head_writes_headers_without_body`, `sftp_list_only_outputs_directory_names`, SFTP range/resume tests, SFTP upload tests, SFTP quote/postquote tests, `version_lists_scp_and_sftp_protocols`, curl corpus 600, 601, 602, 603, 604, 605, 606, 607, 608, 609, 610, 611, 612, 615, 616, 617, 618, 619, 622, 623, 624, 625, 626, 628, 629, 633, 634, 635, 636, 637, 638, 639, 640, 641, 642, 656, 664, 665, 2007, 3021, 3022 | Blocking `ssh2`/libssh2-backed transfers only; supports key auth via `--key`/`--pubkey`, `--knownhosts`, `--hostpubmd5`, `--hostpubsha256`, `--compressed-ssh`, `--insecure`, SCP/SFTP downloads, basic SCP single-file uploads from regular local files with directory URL filename append, stdin/`.` unknown-size rejection, missing remote directory exit 25, empty upload headers, and C-style ignored SCP upload options (`--head`, `--range`, `--continue-at`, `--append`, `--ftp-create-dirs`), SCP download `--range`/`--continue-at` no-op/full-download parity with local output append for nonzero resume offsets, SCP quote/prequote/postquote command options as no-op, SFTP URL and unquoted quote-operand `/~/` remote home expansion, SFTP quote `mtime`/`atime` date parsing for common `curl_getdate` forms including asctime/year-first/compact date and numeric time zones, SFTP simple download byte ranges (`N-M`, `N-`, `-N`), fixed and auto SFTP download resume with output append and already-complete no-op handling, basic SFTP single-file uploads with `--upload-file` including stdin, directory URL filename append, `--list-only` upload precedence, empty upload headers, `--append`, fixed and auto upload resume, `--ftp-create-dirs` parent creation for uploads, SFTP trailing-slash directory listings, `--list-only`, `--head`, `--max-filesize`, output modes, and basic exit mappings for host verification 60, login 67, missing file/path 78, remote access 9, range error 33, bad resume 36, and local upload read errors 26. SFTP normal quote commands run before download/upload/list operations, postquote commands run after successful transfer plus local output/max-filesize handling, `*` accepts remote operation failures, `statvfs` emits header-style data for openable files/directories, trailing junk and unknown commands exit 21, and `+` prequote is ignored for SFTP like C curl. SCP upload glob/parallel and permission edge cases, exact SFTP upload edge cases, exact SFTP quote header callback timing for upload/failure/postquote cases, remaining SFTP quote `curl_getdate` edge cases, remote home expansion edge cases, proxying, password prompts, SSH agent matrix, exact OpenSSH known_hosts hashing behavior, broader corpus mapping, and pytest SSH suite coverage remain incomplete |
| SMB/SMBS | partial | `transfer.rs::run_smb_transfer` | `smb_download_outputs_body_and_sends_expected_sequence`, `smb_url_userinfo_splits_domain_and_decodes_path`, `smb_requires_credentials_returns_67`, `smb_missing_share_path_returns_url_error`, `smb_tree_noaccess_returns_9`, `smb_open_missing_returns_78`, `smb_malformed_read_frame_returns_56`, `smb_head_suppresses_body_and_keeps_zero_code`, `smb_dump_header_is_empty_and_include_adds_no_headers`, `smb_max_filesize_truncates_and_returns_63`, `smbs_is_explicitly_unsupported`, `version_lists_smb_protocol`, curl corpus 1451, 1850 | Plain `smb://` SMB1 fixture-level downloads only; sends negotiate, simplified session setup, tree connect, open, read, close, and tree disconnect, parses share/path with slash-to-backslash file normalization, requires credentials, keeps protocol headers empty, reports zero response code, supports buffered `--max-filesize`, and maps covered auth/access/missing/malformed failures to curl exits plus the impacket-backed corpus smoke cases. Real NTLMv1 authentication, `smbs://`, uploads, directory listing, ranges/resume, proxying, connection reuse, broader corpus impacket server parity, and full SMB error matrix remain incomplete |
| SMTP/SMTPS | partial | `transfer.rs::run_smtp_transfer` | `smtp_upload_sends_mail_transaction_and_dot_stuffs_body`, `smtp_upload_file_adds_size_when_server_advertises_size`, `smtp_default_vrfy_outputs_response_and_code`, `smtp_custom_expn_uses_mail_recipient`, `smtp_custom_expn_adds_smtputf8_when_advertised`, `smtp_recipient_failure_returns_send_error`, `smtp_rcpt_allowfails_continues_when_one_recipient_succeeds`, `smtp_rejects_decoded_control_path`, `version_lists_smtp_protocol`, curl corpus 900, 901, 902, 909, 910, 912, 913, 914, 915, 916, 917, 918, 923, 925, 926, 927, 928, 929, 930, 931, 950, 951, 952, 3002, 3003, 3004, 3005, 3006, 3007 | Plain `smtp://` TCP support only; sends EHLO with URL path domain and HELO fallback, supports unauthenticated `--mail-from`, repeated `--mail-rcpt`, `--mail-rcpt-allowfails`, `-T`/`--upload-file` mail DATA uploads with dot-stuffing and `SIZE`, default HELP/VRFY command mode, custom `-X` command mode including `EXPN` recipient handling with advertised `SMTPUTF8`, SMTP response-code write-out, empty header output, sends `QUIT` on covered send/VRFY error paths, and maps exit code 55 for send failures plus exit 8 for failed VRFY. `smtps://`, STARTTLS, SASL/OAuth/login options, MIME/form mail generation, broader ESMTPUTF8 mailbox/IDN handling, proxying, connection reuse, and full corpus mapping remain incomplete |
| TELNET | partial | `transfer.rs::run_telnet_transfer` | `telnet_upload_file_sends_file_and_outputs_response`, `telnet_sends_stdin_without_upload_file`, `telnet_upload_dash_reads_stdin_and_escapes_iac`, `telnet_filters_negotiation_and_replies_to_peer`, `telnet_writeout_reports_zero_http_code_and_download_size`, `telnet_dump_header_creates_empty_file_and_include_adds_nothing`, `telnet_options_fail_explicitly_until_negotiation_options_are_supported`, `max_filesize_truncates_telnet_body_then_fails`, `version_lists_telnet_protocol`, curl corpus 1326, 1327, 1452, 1548, 1714 | Plain `telnet://` TCP stream support only; stdin/`--upload-file` bytes are sent, outbound IAC bytes are escaped, basic peer-started WILL/DO negotiation is filtered/rejected, `--max-filesize` truncates buffered output and exits 63, and TELNET output remains body-only; `--telnet-option`, proactive C curl negotiation preferences, NAWS/TTYPE/XDISPLOC/NEW_ENV suboptions, interactive polling parity, TLS/proxying, and full corpus mapping remain incomplete |
| TFTP | partial | `transfer.rs::run_tftp_transfer` | `tftp_get_downloads_file_and_sends_default_options`, `tftp_get_acks_oack_and_each_data_block`, `tftp_exact_block_requires_final_empty_block`, `tftp_no_options_sends_plain_rrq`, `tftp_url_double_slash_preserves_leading_filename_slash`, `tftp_writeout_reports_zero_http_code_and_download_size`, `tftp_error_packet_maps_not_found`, `tftp_upload_file_sends_wrq_and_data`, `tftp_upload_timeout_option_uses_connect_deadline`, `tftp_upload_oack_exact_block_sends_final_empty_block`, `tftp_upload_no_options_sends_plain_wrq`, `tftp_upload_no_options_ignores_requested_block_size`, `tftp_upload_stdin_uses_zero_tsize`, `tftp_upload_error_packet_maps_permission`, `tftp_rejects_decoded_nul_path`, `version_lists_tftp_protocol`, curl corpus 271, 283, 284, 285, 286, 332, 1094, 1242, 1243 | Plain `tftp://` RRQ/download and WRQ/upload support only; sends octet RRQ/WRQ with default `tsize`, `blksize`, and `timeout` options, supports C-style initial timeout option derivation from active connect/transfer deadlines, `--tftp-blksize`, `--tftp-no-options`, OACK, DATA/ACK flow in both directions, exact-block final empty DATA handling, upload filename appending for directory URLs, stdin uploads with unknown `tsize`, decoded-NUL rejection, basic TFTP ERROR exit mappings, empty header output, `%{http_code}` zero, `--max-filesize` via buffered download limiting, `;mode=netascii` parsing, and URL double-slash filename semantics. Retransmission/timeout retry parity after the first request, netascii conversion beyond mode suffix stripping, foreign transfer-id exact parity, precise max-filesize block rejection, IPv6/corpus mapping, malformed OACK validation, and full option negotiation remain incomplete |
| WS/WSS | partial | `transfer.rs::run_ws_transfer` | `ws_handshake_sends_upgrade_request`, `ws_non_101_response_returns_http_error`, `ws_text_frame_outputs_payload`, `ws_binary_frame_preserves_raw_payload`, `ws_ping_is_auto_ponged_without_output`, `ws_upload_file_sends_masked_binary_frame_after_get_upgrade`, `ws_upload_stdin_sends_masked_binary_frame`, `ws_invalid_opcode_returns_recv_error`, `ws_include_dump_header_and_writeout_use_handshake_headers`, `version_lists_ws_protocol` | Plain `ws://` TCP support only; sends HTTP/1.1 GET upgrade with WebSocket headers, validates 101 and `Sec-WebSocket-Accept`, outputs decoded text/binary payload bytes, auto-PONGs incoming PING frames, handles CLOSE frames, supports `-T/--upload-file` and `-T -` as masked binary frames after upgrade, dumps/includes handshake headers, reports response code 101 and payload `size_download`, maps non-101 upgrade responses to exit 22, and malformed frames to receive error exit 56. `wss://`, random key generation parity, auth/proxy/cookie/referer integration, `--data*` send semantics, raw mode/API parity, large-frame chunking, compression/extensions/subprotocol validation, precise close/EOF diagnostics, connect-only libcurl API, WebSocket corpus mapping, and pytest WebSocket suite with Rust binary remain incomplete |

## CLI option parity

Every `docs/cmdline-opts/*.md` option must be tracked here or in generated
companion data before this rewrite can be called complete.

| Option or behavior | Status | Rust field/parser | Behavior tests | Notes |
| --- | --- | --- | --- | --- |
| `--help` | partial | `Config::show_help`, `Config::help_category` | `parses_optional_help_category`, `separates_short_help_from_header_alias`, `help_option_without_category_leaves_following_option`, `help_unknown_category_lists_categories`, curl corpus 1462 | Generic help, `-h` short help, and unknown-category category listing are covered; category-specific option help, exact full option text, markdown generation parity, and full help corpus mapping remain incomplete |
| `--config`, default `.curlrc`, `-q` | partial | `cli.rs::insert_config_file` | `tokenizes_curl_config_lines`, `dashed_config_options_do_not_use_colon_separator`, `default_config_continues_from_empty_curl_home_to_xdg_config_home`, `default_config_prefers_curl_home_over_xdg_config_home`, `default_config_skips_unreadable_xdg_candidate`, `default_config_falls_back_to_home_config_dir_without_xdg_config_home`, `default_config_is_skipped_by_disable_flag`, `default_config_is_skipped_by_q_cluster`, curl corpus 462, 774, 2080 | Default config discovery walks `CURL_HOME`, `XDG_CONFIG_HOME/curlrc`, `HOME/.curlrc`, and the no-XDG `.config/curlrc` fallback, skips unreadable candidates like C, and lets `--disable` and leading `-q` clusters suppress default loading; tokenizer covers non-dashed `:`/`=` separators and dashed colon rejection. getpwuid fallback, exact diagnostics, long-option rejection, and full recursive include parity remain incomplete |
| `--variable`, `--expand-*` | partial | `cli.rs::set_variable`, `cli.rs::expand_value` | `expands_command_line_variables`, `expands_variables_with_range_and_base64_functions`, `expands_file_variables_and_json_quotes`, `escaped_invalid_and_missing_variables_match_curl_expansion_shape`, `missing_imported_environment_variable_is_an_error`, `variable_expansion_rejects_unknown_functions_and_raw_nul_bytes`, curl corpus 452, 454, 460, 462, 787 | Supports command-line and config-file variable assignment/import, file/stdin sources, byte ranges, `--expand-` for known value-taking options, escaped `\{{`, missing variable expansion, and `trim`/`json`/`url`/`b64`/`64dec` functions. Full C warning text, all variable corpus mappings, binary non-UTF-8 option payloads, and exact config tokenizer interactions remain incomplete |
| `--next` | partial | `TransferConfig` groups | `splits_transfer_groups_on_next`, `next_requires_url_before_new_group`, `next_rejects_global_options_without_url` | Requires a URL before starting a new operation; needs full option reset audit |
| `--no-*` boolean negation | partial | `cli.rs::parse_no_long` | `no_prefixed_boolean_options_disable_previous_values`, curl corpus 333 | Supported booleans can be disabled and non-boolean negations are rejected |
| `--list-only` | partial | `TransferConfig::list_only` | `parses_list_only_option`, `pop3_list_only_sends_list_for_message_id_without_body`, `ftp_list_only_uses_nlst_without_cwd_for_root`, `sftp_list_only_outputs_directory_names`, `libcurl_writes_source_file_for_supported_options` | Implemented for POP3 message-specific LIST, FTP passive NLST, SFTP directory listings, and emitted in `--libcurl`; FILE behavior and full corpus mapping remain incomplete |
| `--data`, `--data-binary`, `--data-raw`, `--data-urlencode` | partial | `TransferConfig::data` | unit body tests, MQTT publish tests, `http_upload_data_conflict_precedes_upload_file_read`, curl corpus 378 | Basic HTTP body preparation, MQTT QoS 0 publish payloads, and upload/data method-conflict preflight warning only; needs full corpus mapping |
| `--json` | partial | `TransferConfig::data` | `sends_json_body_and_default_json_headers` | Basic defaults only |
| `--url` | partial | `TransferConfig::urls` | `expands_at_file_arguments_while_parsing`, `config_options_use_at_file_expansion`, `url_at_file_downloads_each_url_as_remote_name` | Basic URL list parsing plus command-line/config `@file` expansion with per-file-URL remote-name and no-glob behavior; scheme guessing, `--path-as-is`, exact URL parser diagnostics, and C corpus fixture header-order parity remain incomplete |
| `--url-query` | partial | `TransferConfig::url_query` | `sends_referer_range_and_url_query` | Basic encoding and `+` passthrough only |
| `--request-target` | partial | `TransferConfig::request_target` | `parses_request_target_option`, `config_files_parse_request_target_option`, `request_target_sets_direct_http_request_line`, `request_target_sets_http_proxy_request_line`, `libcurl_writes_request_target_option` | Parses non-empty values, emits `CURLOPT_REQUEST_TARGET`, and uses the verbatim target for plain HTTP raw direct/proxy request lines; HTTPS, redirects, cookie-engine/proxy variants that fall back to reqwest, and full corpus mapping remain incomplete |
| `--form`, `--form-string` | partial | `TransferConfig::forms` | `sends_multipart_form_body`, `missing_form_file_returns_read_error`, curl corpus 41 | Basic fields/files only; missing local form files exit 26 |
| `--upload-file` | partial | `TransferConfig::upload_file` | `telnet_upload_file_sends_file_and_outputs_response`, `telnet_upload_dash_reads_stdin_and_escapes_iac`, `http_upload_file_sends_put_body`, `http_upload_dash_reads_stdin`, `http_upload_to_directory_url_appends_local_filename`, `http_upload_to_query_url_does_not_append_local_filename`, `http_upload_respects_custom_request_method`, `http_upload_rejects_data_body_combination`, `http_upload_data_conflict_precedes_upload_file_read`, `http_upload_missing_file_exits_read_error`, FILE upload tests, FTP upload tests, SCP upload tests, TFTP upload tests, WS upload tests, SFTP upload tests, `libcurl_writes_http_upload_options`, `libcurl_writes_tftp_upload_directory_url`, `libcurl_writes_ftp_upload_append_options`, `parses_upload_file_and_telnet_options`, curl corpus 107, 378 | TELNET upload source plus basic HTTP/HTTPS in-memory PUT upload from a file or stdin, basic FILE local upload from a file, basic FTP passive binary upload from a file or stdin, basic SCP regular-file upload plus C-style stdin/`.` upload failure and ignored SCP upload options, basic TFTP WRQ upload from a file or stdin, basic SFTP file/stdin upload, and basic WebSocket masked binary frame upload from a file or stdin; HTTP upload defaults to PUT, respects explicit `-X`, FTP/SCP/SFTP/TFTP/HTTP append the local basename for directory URLs without queries, FTP maps denied `STOR`/`APPE` and final disk-full replies to covered curl exits, rejects upload/data method conflicts before upload-file reads with the C warning text, reports missing upload files as exit 26, and emits basic `--libcurl` upload options. SCP upload glob/parallel and permission edge cases, FTP active/FTPS/ASCII/CRLF/time-condition edge cases, FILE stdin/directory/create-dirs details, SFTP create-dirs/resume/append modes, upload globs and URL pairing, exact `-T .` matrix beyond WebSocket upgrade-only handling, chunked stdin/`Expect: 100-continue`, HTTP upload resume/`Content-Range`, auth/redirect rewind parity, broader multipart/data mixing parity, parallel timing, and full corpus mapping are missing |
| `--append` | partial | `TransferConfig::ftp_append` | `parses_append_option`, `ftp_upload_append_uses_appe`, `libcurl_writes_ftp_upload_append_options` | Parsed as `-a`/`--append`/`--no-append`, drives FTP upload `APPE`, and emits `CURLOPT_APPEND`; non-FTP protocol interactions and broader corpus mapping remain incomplete |
| FTP passive options | partial | `TransferConfig::ftp_disable_epsv`, `TransferConfig::ftp_skip_pasv_ip` | `parses_ftp_passive_options`, `ftp_default_skips_pasv_ip`, `ftp_epsv_connect_failure_falls_back_to_pasv`, `ftp_disable_epsv_uses_pasv_directly`, `ftp_epsv_reenables_after_disable_epsv`, `ftp_ipv6_disable_epsv_still_uses_epsv`, `ftp_skip_pasv_ip_uses_control_host`, `libcurl_writes_ftp_passive_options` | Parses `--disable-epsv`, `--epsv`, `--no-epsv`, `--ftp-pasv`, and `--ftp-skip-pasv-ip`; skips PASV response IPs by default, falls back from EPSV to PASV on data-connect failure, skips EPSV when requested except for IPv6, and emits `CURLOPT_FTP_USE_EPSV`/`CURLOPT_FTP_SKIP_PASV_IP` when explicitly configured. Active mode, `--ftp-port`, EPRT toggles, connection reuse state, and full corpus mapping remain incomplete |
| `--mail-from`, `--mail-rcpt`, `--mail-rcpt-allowfails` | partial | `TransferConfig::mail_from`, `TransferConfig::mail_rcpt`, `TransferConfig::mail_rcpt_allowfails` | `parses_mail_options`, SMTP integration tests, `libcurl_writes_smtp_mail_options` | Basic SMTP sender/recipient command handling and libcurl emission only; `--mail-auth`, `--login-options`, SASL options, SMTPUTF8/address validation details, DSN corpus mapping, and non-SMTP interactions remain incomplete |
| SSH options | partial | `TransferConfig::ssh_private_key`, `ssh_public_key`, `ssh_known_hosts`, `ssh_hostpubmd5`, `ssh_hostpubsha256`, `compressed_ssh`, `insecure` | `parses_ssh_options`, SCP/SFTP integration tests | `--key`, `--pubkey`, `--knownhosts`, `--hostpubmd5`, `--hostpubsha256`, `--compressed-ssh`, and `--insecure` are parsed, emitted in `--libcurl` where applicable, and used by the SCP/SFTP transfer path. Prompting, default key discovery, SSH config files, hashed known_hosts parity, hostpub formatting matrix, password auth fixtures, and full SSH option corpus mapping remain incomplete |
| `--tftp-blksize`, `--tftp-no-options` | partial | `TransferConfig::tftp_blksize`, `TransferConfig::tftp_no_options` | `parses_tftp_options`, `rejects_bad_tftp_blksize_values`, TFTP integration tests, `libcurl_writes_tftp_options` | Basic TFTP RRQ/WRQ option emission and libcurl output only; exact C timeout option derivation, retry negotiation, malformed OACK handling, and full corpus mapping remain incomplete |
| `--telnet-option` | partial | `TransferConfig::telnet_options` | `telnet_options_fail_explicitly_until_negotiation_options_are_supported`, `parses_upload_file_and_telnet_options` | Parsed and rejected explicitly for TELNET until TTYPE/XDISPLOC/NEW_ENV/NAWS negotiation support is implemented |
| `--ipfs-gateway` | partial | `TransferConfig::ipfs_gateway` | `parses_ipfs_gateway`, `rejects_empty_ipfs_gateway`, IPFS/IPNS rewrite tests | Explicit gateway parsing and IPFS/IPNS HTTP rewrite support only; generated libcurl rewrites URL through the same helper, but full corpus mapping and exact C URL parser edge cases remain incomplete |
| `--output`, `--remote-name`, `--remote-header-name` | partial | output module | output and `-OJ` tests, `remote_header_name_with_remote_name_refuses_to_overwrite_header_filename`, `remote_header_name_with_remote_name_strips_header_filename_path`, curl corpus 1460 | Header-derived remote filenames refuse to overwrite existing files and strip path components; needs full Content-Disposition parity |
| `--dump-header` | partial | `TransferConfig::dump_header` | `dump_header_dash_writes_headers_to_stdout`, `dump_header_records_manual_redirect_history`, `ftp_dump_header_writes_control_replies_and_include_adds_no_bytes` | HTTP/file header dumping, manual HTTP redirect header history, and FTP control-reply dumping are covered; reqwest-managed redirect header history and exact FTP `-D -` interleaving remain incomplete |
| `--write-out` | partial | `writeout.rs` | `renders_common_variables`, `render_segments_switches_output_streams`, `write_out_at_file_reads_format`, curl corpus 1278 | Common variables, parser-time `@file` format loading, and `%{stdout}`/`%{stderr}` stream switching are covered; many variables remain missing |
| `--referer` | partial | `TransferConfig::referer`, `TransferConfig::auto_referer` | `sends_referer_range_and_url_query`, `location_does_not_auto_referer_without_referer_auto`, `fixed_referer_is_reused_across_redirects_without_auto`, `auto_referer_strips_credentials_and_fragment_on_redirect`, `auto_referer_tracks_immediately_previous_redirect_url`, `auto_referer_redirect_rewrites_post_to_get`, `auto_referer_respects_max_redirs_limit`, `initial_referer_auto_replaces_referer_after_redirect`, `custom_referer_header_suppresses_generated_referer` | Basic header, `;auto` redirect behavior, default POST-to-GET method rewrite on followed 301/302 auto-referer redirects, `%{referer}`, max-redirs exhaustion for the auto path, and libcurl `CURLOPT_AUTOREFERER` covered; auth/cookie redirect scoping and full corpus mapping remain incomplete |
| `--range` | partial | `TransferConfig::range` | `file_range_outputs_slice`, HTTP header test, SCP/SFTP range tests | HTTP/file/SFTP byte ranges plus SCP download no-op/full-body parity; multipart ranges and broader resume interactions missing |
| `--etag-save`, `--etag-compare` | partial | `TransferConfig::etag_save`, `TransferConfig::etag_compare` | `etag_compare_sends_if_none_match`, `etag_save_writes_response_etag` | Single URL semantics and full corpus mapping missing |
| `--time-cond` | partial | `TransferConfig::time_cond` | `time_cond_sends_if_modified_since`, `negative_time_cond_sends_if_unmodified_since` | Date parsing and file mtime semantics missing |
| `--user` | partial | `TransferConfig::user` | `username_only_basic_auth_encodes_empty_password`, `ldap_simple_bind_uses_user_option` | Basic HTTP auth and LDAP simple bind credentials only |
| `--oauth2-bearer` | partial | `TransferConfig::oauth2_bearer` | `sends_oauth2_bearer_authorization_header` | Redirect credential scoping needs parity work |
| `--disallow-username-in-url` | partial | `TransferConfig::disallow_username_in_url`, generated libcurl `CURLOPT_DISALLOW_USERNAME_IN_URL` | `disallow_username_in_url_rejects_url_userinfo`, `renders_disallow_username_in_url_option`, `libcurl_writes_source_file_for_supported_options`, curl corpus 2075 | URLs containing credentials are rejected with exit 67 before network work and generated libcurl output is covered; interactions with redirects and non-HTTP schemes need broader parity |
| `--proto-default` | partial | `TransferConfig::proto_default`, `glob::apply_default_protocol` | `parses_proto_default_option`, `applies_default_protocol_to_schemeless_urls`, curl corpus 1146, 2008, 2044, 2045 | Adds a configured default scheme before URL glob expansion and validates known protocol names case-insensitively; schemeless HTTP proxy absolute-form requests are covered for the mapped case |
| `--resolve` | partial | `TransferConfig::resolve`, reqwest DNS overrides, generated libcurl `CURLOPT_RESOLVE` | `resolve_maps_host_to_address`, `resolve_invalid_syntax_exits_option_syntax_error`, `ip_version_filters_resolved_address_families`, `renders_resolve_options`, `libcurl_writes_source_file_for_supported_options`, curl corpus 3019 | Basic `host:port:address[,address]` parsing and HTTP DNS override support are covered, including invalid syntax exit 49, `--ipv4`/`--ipv6` family filtering, and generated libcurl slist output; removal entries are accepted as no-ops, and wildcard/IPv6-host/timeout parity remain incomplete |
| `--connect-to` | partial | `TransferConfig::connect_to`, generated libcurl `CURLOPT_CONNECT_TO` | `connect_to_invalid_syntax_exits_option_syntax_error`, `connect_to_remaps_plain_http_destination`, `connect_to_nonmatching_rule_does_not_remap_or_fail`, `renders_connect_to_options`, `libcurl_writes_source_file_for_supported_options`, curl corpus 3020 | Invalid port syntax exits 49; matching rules remap plain direct `http://` TCP destinations while preserving the original URL and `Host` header, nonmatching rules are ignored, and generated libcurl slist output is covered. HTTPS, proxies, redirects, multipart uploads, IPv6 rule syntax, and full corpus mapping remain incomplete |
| `--proxy` | partial | `TransferConfig::proxy` | `proxy_user_sets_proxy_authorization_header`, `noproxy_bypasses_configured_proxy`, curl corpus 2008 | Basic HTTP proxy path only; simple explicit `http://` proxies use a raw request path to preserve curl-like wire headers, while broader proxy behavior still delegates to reqwest or remains missing |
| `--proxy-user` | partial | `TransferConfig::proxy_user` | `proxy_user_sets_proxy_authorization_header` | Basic proxy auth only |
| `--noproxy` | partial | `TransferConfig::noproxy` | `noproxy_bypasses_configured_proxy` | Delegates matching to reqwest |
| `--compressed` | partial | `TransferConfig::compressed` | `downloads_http_and_renders_writeout`, `compressed_decodes_supported_response_bodies`, `compressed_custom_accept_encoding_still_decodes_response_body`, `compressed_include_preserves_encoded_response_headers`, `compressed_dump_header_preserves_encoded_response_headers`, `compressed_unknown_content_encoding_returns_bad_content_encoding`, `raw_http_compressed_request_target_decodes_body_and_preserves_headers`, `http_does_not_decode_content_encoding_without_compressed`, `generated_headers_respect_non_empty_custom_overrides` | Sends curl-compatible `Accept-Encoding: deflate, gzip, br` when no custom header is present, suppresses default negotiation and decoding for ordinary HTTP requests, preserves explicit `Accept-Encoding` while still decoding, decodes gzip/deflate/brotli response bodies on reqwest and raw HTTP paths, preserves encoded response headers under `--include`/`--dump-header`, and maps unknown body encodings to exit 61; zstd availability, stacked encoding matrices, transfer-encoding decoding beyond existing chunked handling, exact diagnostics, header/body accounting edge cases, and full corpus mapping remain incomplete |
| `--location` | partial | reqwest redirect policy plus manual redirect loop for `;auto`/post-redir options | `location_does_not_auto_referer_without_referer_auto`, `post301_preserves_post_on_redirect`, `post302_preserves_post_on_redirect`, `post303_preserves_post_on_redirect`, `post301_follows_300_redirect_and_replays_post_body`, `custom_method_post_redirect_drops_body_when_post_flag_does_not_apply` | Basic following, auto-referer, max-redirs, manual-loop 300-399 redirects, POST preservation toggles, and custom-method POST body dropping when a post-redir flag does not apply are covered; full redirect auth/cookie/header/body rewind semantics and remaining custom-method matrices remain incomplete |
| `--fail`, `--fail-with-body` | partial | transfer status handling | `fail_suppresses_http_error_body`, `fail_with_body_outputs_http_error_body_and_fails`, `fail_after_fail_with_body_suppresses_error_body`, `fail_with_body_after_fail_outputs_error_body`, `no_fail_with_body_disables_http_error_failure`, `fail_include_outputs_headers_without_error_body`, `fail_writeout_reports_zero_delivered_body_bytes`, `fail_with_body_retry_outputs_failed_and_successful_bodies`, `fail_options_are_mutexed_with_last_one_winning` | Basic HTTP 4xx/5xx exit 22 handling is covered; `--fail` suppresses bodies while preserving included headers and zero delivered body bytes, `--fail-with-body` preserves bodies, retry preserves failed bodies for stdout, and option mutex ordering follows C warnings. Auth negotiation edge cases, 416 resume exception, exact stderr/`%{errormsg}` wording, output-file retry truncation parity, and full corpus mapping remain incomplete |
| `--silent`, `--show-error`, `--no-progress-meter` | partial | `TransferConfig::silent`, `TransferConfig::show_error` | `parses_no_progress_meter_as_silent_progress_suppression` | Progress meter output is not implemented, but `--no-progress-meter` is accepted for harness/corpus compatibility |
| `--dump-header` | partial | `TransferConfig::dump_header`, `output::dump_headers`, `output::append_dump_headers` | `dump_header_dash_writes_headers_to_stdout`, `dump_header_records_manual_redirect_history`, `dump_header_missing_parent_exits_write_error`, curl corpus 419 | Header dumping is wired for HTTP and implemented protocols, with missing parent paths failing as write errors unless `--create-dirs` is used and manual HTTP redirects appending intermediate headers; append semantics across repeated transfers, reqwest-managed redirect header history, `%` stderr output, and exact header casing remain incomplete |
| `--connect-timeout`, `--max-time` | partial | reqwest client builder | curl corpus 1410 | Negative `--max-time` is rejected; runtime timeout parity still needs edge case tests |
| `--ipv4`, `--ipv6` | partial | `TransferConfig::ip_version` | `parses_ip_version_short_aliases`, `config_files_parse_ip_version_options`, `ip_version_filters_resolved_address_families` | `-4`/`--ipv4` and `-6`/`--ipv6` parse with last-option-wins semantics, filter `--resolve` address families, constrain reqwest DNS resolution, and are applied by the shared raw TCP connect helper plus generated libcurl `CURLOPT_IPRESOLVE`; connection reuse, proxy remapping, wildcard `--resolve`, and broader corpus coverage remain incomplete |
| `--max-filesize` | partial | `TransferConfig::max_filesize` | `parses_max_filesize_units_and_fractions`, `rejects_bad_max_filesize_values`, `max_filesize_allows_http_body_within_limit`, `max_filesize_zero_disables_limit`, `max_filesize_rejects_http_content_length_before_body_output`, `max_filesize_truncates_unknown_http_body_then_fails`, `max_filesize_does_not_fail_head_with_large_content_length`, `max_filesize_truncates_telnet_body_then_fails`, `mqtt_max_filesize_rejects_large_publish_before_output`, `ftp_max_filesize_rejects_after_size_before_retr`, `libcurl_writes_source_file_for_supported_options` | Parses curl-style bytes plus `b/K/M/G/T/P` suffixes and fractional unit values, with `0` disabling the limit; HTTP known `Content-Length`, FTP `SIZE`, and MQTT PUBLISH remaining length fail with exit 63 before body output, unknown-size HTTP/TELNET/FTP buffered bodies write up to the limit then fail, and `--libcurl` emits `CURLOPT_MAXFILESIZE_LARGE`. Overflow `Content-Length`, exact C stderr variants, true streaming cutoff, decompressed `--compressed` accounting, redirect/retry edge cases, and full corpus mapping remain incomplete |
| `--http0.9`, `--no-http0.9` | partial | `TransferConfig::http09_allowed` | `parses_http09_boolean_option`, `config_files_parse_http09_boolean_option`, `http09_allows_headerless_response`, `http09_response_is_denied_by_default`, `no_http09_denies_headerless_response_after_enable`, `http09_denial_with_writeout_uses_unsupported_protocol_exit`, `raw_http09_response_requires_opt_in`, `raw_http09_allows_headerless_response`, `raw_http09_head_is_weird_reply`, `libcurl_writes_request_target_option` | Disabled by default, parses boolean enable/disable, enables reqwest HTTP/0.9 responses, handles headerless raw HTTP bodies only when opted in, preserves response code `000`, maps raw HEAD HTTP/0.9 to weird-reply exit 8, fails plain HTTP headerless responses with exit 1 when disabled, keeps write-out exit reporting on the denial path, and emits `CURLOPT_HTTP09_ALLOWED`; exact stderr text, HTTPS coverage, proxy variants, and full corpus mapping remain incomplete |
| `--http1.0`, `--http1.1`, `--http2` | partial | reqwest versions | `distinguishes_http2_modes`, `tls_short_aliases_are_not_http_version_aliases`, `http2_does_not_force_prior_knowledge` | `-0` maps to HTTP/1.0, while `-1`/`-2`/`-3` are reserved for TLS/SSL aliases like C curl; HTTP/3 missing |
| `--tlsv1*`, `--sslv2`, `--sslv3` | partial | `TransferConfig::ssl_version` | `tls_short_aliases_are_not_http_version_aliases` | `--tlsv1`, `--tlsv1.0`, `--tlsv1.1`, `--tlsv1.2`, `--tlsv1.3`, and `-1` set the HTTP client minimum TLS version and emit `CURLOPT_SSLVERSION` in generated libcurl; deprecated SSLv2/SSLv3 aliases are accepted as warning-only no-ops. `--tls-max`, proxy TLS version selection, backend-specific TLS downgrade behavior, and corpus coverage remain incomplete |
| `--retry*` | partial | `TransferConfig::retry` | `retries_transient_http_status_then_succeeds`, `retry_all_errors_retries_failed_http_status`, curl corpus 1269 | HTTP transient status retry and basic retry timers only; FTP, upload rewind, partial-output resume, and full error taxonomy missing |
| `--continue-at` | partial | `TransferConfig::continue_at` | `continue_at_fixed_offset_sends_range_and_appends_output`, `continue_at_auto_uses_existing_output_size`, `continue_at_fixed_offset_to_stdout_sends_range`, `continue_at_http_200_without_content_range_fails_without_appending`, `continue_at_http_200_matching_existing_length_is_already_complete`, `continue_at_http_416_with_fail_is_already_complete`, `continue_at_resumes_file_url_output`, FTP download resume tests, FTP upload resume tests, SCP/SFTP download resume tests, `http_data_rejects_continue_at_combination`, curl corpus 426, 1409 | Download resume for HTTP/file/FTP/SFTP output and stdout plus FTP/SFTP upload resume; SCP downloads accept fixed/auto resume but read the full remote body like C curl and append to local output when the resume offset is nonzero. Invalid offsets are rejected where the protocol can enforce them, `--data`/`--json` with resume is rejected as a usage error, HTTP GET resume requires matching `Content-Range` unless the remote size equals the resume offset or the server returns 416 for an already complete resume, FTP download resume sends `REST` after `SIZE` with known-size complete/beyond-size handling plus REST failure exit 31, and FTP upload resume skips local bytes then uses `APPE` with optional remote `SIZE` for `-C -`. FTP finite byte ranges, HTTP upload resume, partial-output retry resume, redirect resume edge cases, and remote-header filename auto-resume incomplete |
| `--cookie`, `--cookie-jar`, `--junk-session-cookies` | partial | `TransferConfig::cookie`, `TransferConfig::cookie_files`, `TransferConfig::cookie_jar`, `TransferConfig::junk_session_cookies` | `empty_cookie_input_activates_cookie_engine`, `cookie_header_appends_repeated_literals`, `cookie_file_sends_netscape_cookie`, `junk_session_cookies_ignores_file_session_cookies`, `cookie_file_and_literal_cookie_are_combined`, `cookie_jar_saves_response_cookies`, `cookie_jar_creates_header_only_file_without_cookies`, `cookie_jar_sends_cookie_on_later_url_in_group` | Basic direct Cookie headers, repeated literal joining, engine activation, Netscape/Set-Cookie file import, session-cookie skipping for file import, HTTP cookie engine, file-plus-literal sends, and Netscape jar output only; PSL/supercookie rejection, exact expiry/order/domain/path/sorting parity, redirect-cookie export, and full corpus mapping remain incomplete |
| `--parallel`, `--parallel-max`, `--parallel-immediate`, `--parallel-max-host` | partial | `Config::parallel*`, `transfer.rs::run_parallel` | `parses_parallel_options_as_global_state`, `parallel_runs_transfers_concurrently_to_files` | Basic bounded async scheduling for expanded URL jobs; `--parallel-immediate` and `--parallel-max-host` are parsed/stored but no-op, stdout/write-out/cookie timing/order, retry queue behavior, fail-early, multiplexing, and full corpus parity remain incomplete |
| `--libcurl` | partial | `Config::libcurl`, `libcurl.rs` | `parses_libcurl_options_as_global_state`, `libcurl_writes_source_file_for_supported_options`, `libcurl_writes_curl_compatible_default_user_agent`, `libcurl_writes_ftp_upload_append_options`, `libcurl_writes_ftp_passive_options`, `libcurl_writes_ftp_quote_options` | Emits C source for supported HTTP/file/FTP/TFTP/SFTP URL expansion and common scalar/string/slist options, including curl-compatible default user-agent output, HTTP version, TLS version, IP resolve, request target, HTTP/0.9, upload, FTP append, FTP passive, and FTP quote/prequote/postquote options. The FTP quote `--libcurl` corpus fixture 1405 remains unmapped because exact generated source formatting and harness-option parity still differ from C curl. Exact C curl formatting, callbacks/upload source wiring, multipart MIME source, protocol-specific options, multi-transfer ordering, and full fixture parity remain incomplete |
| `--trace`, `--trace-ascii`, `--trace-time` | partial | parser compatibility only | `accepts_trace_options_for_corpus_runner`, curl corpus smoke tests | Accepted so `tests/runtests.pl` can wrap Rust-sidecar transfers; trace file generation and trace-time output are not implemented |
| URL parser/preflight errors | partial | transfer dispatch and URL parsing | `unknown_url_scheme_exits_unsupported_protocol`, curl corpus 23, 219, 467, 496, 1234, 1236, 1264, 1281, 1289, 1474, 2092 | Unknown `scheme://` URLs fail before HTTP transfer dispatch with curl's unsupported-protocol exit code, disabled protocols are rejected before transfer, malformed host/port/glob inputs are rejected before transfer, bad option spellings fail in parsing, and missing local upload files fail before HTTP transfer; exact stderr wording and all URL parser edge cases still need broader corpus coverage |

## Build and test parity

| Gate | Status | Evidence |
| --- | --- | --- |
| Cargo locked build/test | partial | `cargo test -p curl-rust --locked` |
| Cargo clippy | partial | `cargo clippy -p curl-rust --all-targets --locked -- -D warnings` |
| CMake sidecar build | partial | `BUILD_RUST_CURL_EXE=ON`, `curl-rust-test`, `curl-rust-corpus-test` |
| Autotools sidecar build | partial | `--enable-rust-curl`, `make -C rust rust-test`, `make -C rust rust-corpus-test` |
| Rust sidecar install/package behavior | partial | CMake and Autotools install the sidecar as `curl-rust` when `BUILD_RUST_CURL_EXE=ON` or `--enable-rust-curl` is enabled; broader downstream package metadata and installer coverage still need audit |
| GitHub Actions sidecar gate | partial | `.github/workflows/linux.yml` `Rust sidecar` job runs locked Cargo fmt/test/clippy/build/version checks plus CMake and Autotools sidecar build/test/corpus targets |
| Existing curl Perl suite with C binary | verified for C path | Does not prove Rust parity |
| Existing curl Perl suite with Rust binary | partial | `curl-rust-corpus-test` maps 249 smoke cases across FILE, FTP, DICT, GOPHER, HTTP, IPFS/IPNS, IMAP, MQTT, POP3, SCP/SFTP, SMB, SMTP, TFTP, TELNET, CLI help/config/options, and URL/HTTP preflight/error cases; broader protocol corpus remains required before full parity |
| Pytest HTTP suite with Rust binary | missing | Required before full parity |

Current `curl-rust-corpus-test` selection:

- FILE: 200, 201, 202, 203, 204, 205, 231, 1016, 1017, 1018, 1019, 1020, 1146, 1220, 2072
- FTP: 100, 102, 104, 107, 113, 114, 115, 117, 118, 120, 121, 124, 125, 126, 136, 137, 138, 147, 148, 225, 226, 227, 237, 238, 261, 290, 291, 295, 754, 1000, 1003, 1005, 1006, 1062, 1153, 1219, 1224, 1282, 2045
- HTTP: 1460, 2008
- IMAP: 800, 801, 802, 803, 806, 807, 808, 809, 810, 811, 812, 813, 814, 817, 818, 829, 841, 846, 847, 1847, 1848, 3206
- DICT: 1450
- GOPHER: 1200, 1201, 1202, 1203
- CLI/help/config/options: 333, 452, 454, 460, 462, 467, 697, 759, 774, 787, 1022, 1023, 1027, 1269, 1278, 1409, 1410, 1462, 1474, 2044, 2080
- URL/HTTP preflight/error paths: 21, 23, 41, 219, 378, 419, 426, 496, 1234, 1236, 1264, 1281, 1289, 1673, 2075, 2092, 3019, 3020
- IPFS/IPNS: 723, 725, 726, 738, 739, 741
- MQTT: 1190, 1191, 1192, 1193, 1194, 1196, 1198, 1199, 2200, 2201, 2202, 2203, 2204, 2206, 2207, 3017, 3018
- POP3: 850, 851, 852, 853, 854, 855, 856, 857, 858, 859, 860, 861, 862, 863, 875, 894, 993, 997
- SCP/SFTP: 600, 601, 602, 603, 604, 605, 606, 607, 608, 609, 610, 611, 612, 615, 616, 617, 618, 619, 622, 623, 624, 625, 626, 628, 629, 633, 634, 635, 636, 637, 638, 639, 640, 641, 642, 656, 664, 665, 2007, 3021, 3022
- SMB: 1451, 1850
- SMTP: 900, 901, 902, 909, 910, 912, 913, 914, 915, 916, 917, 918, 923, 925, 926, 927, 928, 929, 930, 931, 950, 951, 952, 3002, 3003, 3004, 3005, 3006, 3007
- TFTP: 271, 283, 284, 285, 286, 332, 1094, 1242, 1243
- TELNET: 1326, 1327, 1452, 1548, 1714
